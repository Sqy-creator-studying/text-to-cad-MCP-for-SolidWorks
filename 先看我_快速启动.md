# EmbodiedCAD 快速启动

1. 解压压缩包。
2. 进入解压后的文件夹。
3. 双击 `启动_EmbodiedCAD.bat`。
4. 首次运行会自动创建 `.venv` 并安装 Python / 前端依赖，请耐心等待。
5. 浏览器自动打开 `http://127.0.0.1:5173` 后即可操作。

如果双击脚本被系统拦截，可以尝试双击 `start_embodiedcad.bat`，或右键选择“使用 PowerShell 运行” `start_embodiedcad.ps1`。

详细操作步骤请阅读 `EmbodiedCAD_软件使用指南.md`。
