# EmbodiedCAD 项目证明材料

## 项目名称
**EmbodiedCAD：面向具身系统的文本驱动 CAD 生成与工程设计交互平台**

## 项目简介
EmbodiedCAD 是我持续参与和推进的一个文本驱动工程设计项目，目标是探索：当用户只提供自然语言需求时，系统能否进一步理解设计意图、调用工程工具、生成可验证的 CAD 结果，并支持后续迭代修改。围绕这一目标，项目将自然语言输入、参数化 CAD 建模、三维预览、几何引用与工程交互连接成一个完整闭环。

与单纯“生成一个模型”不同，EmbodiedCAD 更关注生成过程是否具备可解释性、可修改性和工程可落地性。系统支持 STEP、STL、GLB、URDF 等结果导出，并通过本地 CAD Explorer / EmbodiedCAD 工作台对生成结果进行浏览、检查和交互。

## 核心问题意识
我在这个项目中最关注的问题是：未来重要的大模型系统，也许不只是最会聊天的模型，而是最能在复杂环境中持续行动和学习的模型。对于 CAD 设计任务来说，真正困难的并不是一次性生成一个“看起来合理”的结果，而是如何把一句自然语言逐步转化为参数、组件、装配关系、几何结构和工程约束，并在工具反馈中不断修正结果。

这种问题意识使我越来越关注从“多模态理解”走向“智能体行动”的路径，即让模型不仅生成内容，还能够在真实工具链和交互环境中持续修正自己的决策。

## 系统方案
EmbodiedCAD 的整体链路可以概括为：

**自然语言需求 -> Python 参数化 CAD 源文件 -> STEP / GLB / 拓扑数据生成 -> Web 端三维预览与交互 -> 几何引用与后续迭代**

项目结合 `build123d` 与 `OpenCascade` 生成几何结果，通过本地 Web Viewer 对模型进行三维浏览和检查，并支持稳定的 `@cad[...]` 几何引用，以便进行更精细的后续编辑。项目还进一步延伸到了无人机、机械臂、组件化设计等方向，尝试把 CAD 生成与具身系统设计结合起来。

## 我的具体贡献
在这一项目中，我主要参与并推进了以下工作：

- 持续理解并推进项目整体架构，将自然语言输入、CAD 生成、三维预览和工程交互串联起来；
- 参与 EmbodiedCAD 前端工作台的多轮迭代，围绕组件库、装配树、3D 工作区、Inspector、Assistant 等模块优化交互逻辑；
- 强化“可控式助手”思路，即让系统先预览操作、再验证、再应用，而不是直接执行不可控行为；
- 推进 UI/UX 层面的信息层级优化，使系统更像一个专业工程工作台，而不只是调试界面；
- 新增中英文切换能力，提升系统在展示和沟通中的完整度；
- 在真实修改、构建和验证过程中持续保证系统可运行、交互一致，并通过 `ui_smoke` 等方式完成验证。

## 项目创新性与能力体现
我认为 EmbodiedCAD 的价值不只在于“做出了一个 CAD 生成工具”，更在于它尝试打通了以下几层能力：

1. **从自然语言到工程对象的映射**
   把抽象需求逐步落到参数、结构、装配和导出结果上。

2. **从一次性生成到可迭代设计**
   系统不是只给出一个静态结果，而是为后续编辑、验证和迭代保留接口。

3. **从模型能力到工具链闭环**
   将大模型 / 智能体思路与真实工程工具结合，而不是停留在纯对话层。

4. **面向具身系统的扩展潜力**
   项目进一步延伸到无人机、机械臂和工程组件化设计等方向，体现出从软件生成走向物理系统设计的可能性。

通过这个项目，我锻炼和体现的能力主要包括：将抽象问题转化为系统方案的能力、跨越 AI 与工程设计的综合理解能力、在真实项目中持续推进和完善原型的能力，以及把技术问题组织清楚并逐步落地的能力。

## 与申请方向的相关性
EmbodiedCAD 与多模态大模型、AIGC、智能体和反馈闭环学习等方向高度相关。它让我更加明确地意识到，未来的智能系统不应只停留在信息生成层面，而应逐步具备在真实工具链和复杂环境中持续交互、修正并完成任务的能力。这也是我希望继续深入探索的方向。

## 可附材料
- GitHub 仓库链接
- 项目界面截图 / Demo
- 项目说明 PDF
- 如有需要，可附系统结构图或关键模块说明
